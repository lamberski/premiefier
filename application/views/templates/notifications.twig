<script id="notifications-template" type="x-handlebars-template">
  {% raw %}
    {{#if notifications}}
      <section>
        {{#each notifications as |notification notificationId|}}
          <article class="notification">
            <img src="{{notification.premiere.poster_url}}" alt="">
            <div class="notification__details">
              <h1>{{notification.premiere.title}}</h1>
              <p class="movie__release-date">
                Will be released on <time>{{notification.premiere.released_at}}</time>.
              </p>
              <p class="movie__rotten-link">
                <a href="{{notification.premiere.details_url}}">View on Rotten Tomatoes &rarr;</a>
              </p>
              <form method="delete" action="api/notifications" class="form">
                <input type="hidden" name="notification_id" value="{{notification.id}}">
                <button type="submit" data-primary="Unsubscribe" data-secondary="Subscribe" data-processing="Processing&hellip;">Unsubscribe</button>
              </form>
            </div>
          </article>
        {{/each}}
      </section>
    {{else}}
      {{#unless error}}
        <section class="text">
          <p><strong>Empty list</strong>. You are not subscribed to any movie premiere yet.</p>
        </section>
      {{/unless}}
    {{/if}}
  {% endraw %}
</script>
