<form method="post" action="{{ app.request.getUriForPath('/search') }}">
    <fieldset>
        <input type="search" name="title" placeholder="Enter movie title here" value="{{ title }}" autofocus>
        <input type="submit" value="Search" data-initial="Search" data-processing="Searching&hellip;">
    </fieldset>

    {% if title %}
        {% if not movie.wasFound %}
            <p>Movie was not found.</p>
        {% elseif movie.releasedAt == 'N/A' %}
            <p>Release date of this movie is not available.</p>
        {% elseif movie.isAlreadyReleased %}
            <p>This movie was already released.</p>
        {% endif %}
    {% else %}
        <p>Enter movie title.</p>
    {% endif %}
</form>
